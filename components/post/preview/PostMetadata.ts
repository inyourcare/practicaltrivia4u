export interface PostMetadata {
  id: string;
  tags: Array<string>;
  category: Array<string>;
  date: string;
  title: string;
  description: string;
  image: string;
  imageAlt: string;
  author: string;
}
